spring:
  banner:
    location: banner.txt
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: schema.sql



